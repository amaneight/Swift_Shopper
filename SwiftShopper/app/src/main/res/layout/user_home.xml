<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical"
    android:gravity="center_horizontal">

    <!--  Sample Dashboard screen with Two buttons -->
    <!--  Button to view all products screen -->
    <Button android:id="@+id/btnScanProduct"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="Scan Product"
        android:layout_marginTop="30dip"/>

    <Button android:id="@+id/btnCurrentCart"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="Current Cart"
        android:layout_marginTop="30dip"/>

    <!--  Button to create a new product screen -->
    <Button android:id="@+id/btnPreviousPurchases"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="Previous Purchases"
        android:layout_marginTop="30dip"/>

    <!--  Button to create a new product screen -->
    <Button android:id="@+id/btnLogout"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="Logout"
        android:layout_marginTop="30dip"/>

</LinearLayout>